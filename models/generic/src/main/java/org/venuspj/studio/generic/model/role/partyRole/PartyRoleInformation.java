package org.venuspj.studio.generic.model.role.partyRole;

import org.venuspj.studio.generic.fundamentals.name.Name;
import org.venuspj.studio.generic.model.role.RoleInformation;

public class PartyRoleInformation extends RoleInformation {
    protected PartyRoleInformation(Name aRoleName) {
        super(aRoleName);
    }
}
