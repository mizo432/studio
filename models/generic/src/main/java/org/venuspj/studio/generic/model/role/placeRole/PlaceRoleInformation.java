package org.venuspj.studio.generic.model.role.placeRole;

import org.venuspj.studio.generic.fundamentals.name.Name;
import org.venuspj.studio.generic.model.role.RoleInformation;

public class PlaceRoleInformation extends RoleInformation {
    protected PlaceRoleInformation(Name aRoleName) {
        super(aRoleName);
    }
}
