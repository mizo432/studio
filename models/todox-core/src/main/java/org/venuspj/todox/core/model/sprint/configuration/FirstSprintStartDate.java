package org.venuspj.todox.core.model.sprint.configuration;

import org.venuspj.studio.generic.fundamentals.datetime.RecordDate;

import java.time.LocalDate;

public class FirstSprintStartDate extends RecordDate {
    public FirstSprintStartDate(LocalDate aValue) {
        super(aValue);
    }
}
